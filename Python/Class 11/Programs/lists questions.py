#WAP to count the number of strings whose  length is more than 2 and the first and last letter
#are same from a given list of strings
#n=int(input('Enter the limit : '))
#l=[input('Enter the string : ') for i in  range(n)]
#c=0
#for i in l:
#    if len(i)>=2 and i[0]==i[-1]:
#        c+=1
#print('The number of strings is ',c)

#Function to swap first half and second half
#n=int(input('Enter the limit : '))
#l=[eval(input('Enter the element : ')) for i in range(n)]
#m=n//2
#print(l[m:]+l[:m])

